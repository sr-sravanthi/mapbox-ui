export interface UserRequest {
   
    userid?: string,
    deviceId?: string,
    apitokenId?: string,
    timestamp?:string
}
export interface personalAwardEntity{

        isLoggedUser?:boolean,
        userName?:string,
        rank?:number,
        score?:number,
        level?:boolean
       
    }
